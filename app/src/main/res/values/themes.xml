<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch.Global" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    </style>

    <!-- STYLE FOR MAIN ACTIVITY -->
    <style name="Main.NoTitle" parent="Theme.Go4Lunch.Global">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="actionBarTheme">@null</item>

        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <!-- STYLE SIGN-IN ACTIVITY -->
    <style name="SignIn" parent="Theme.Go4Lunch.Global">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="actionBarTheme">@null</item>

        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor">@android:color/black</item>
    </style>

    <!-- STYLE FOR HOME ACTIVITY -->
    <style name="Home" parent="Theme.Go4Lunch.Global">
        <item name="colorPrimary">@color/red_500</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- HOME -> NAVIGATION DRAWER -> Paramètres -->
        <item name="colorSecondary">@color/quantum_grey</item>

        <!-- HOME -> BottomNavigationView -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>

        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <item name="android:statusBarColor">@color/red_700</item>
    </style>
</resources>